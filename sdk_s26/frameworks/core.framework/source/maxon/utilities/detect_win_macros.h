#ifdef DELETE
	#error DELETE macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef INTERFACE
	#error INTERFACE macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef GetFirstChild
	#error GetFirstChild macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef GetMessage
	#error GetMessage macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef CreateWindow
	#error CreateWindow macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef GetObject
	#error GetObject macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef AddJob
	#error AddJob macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef GetClassInfo
	#error GetClassInfo macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef PASSTHROUGH
	#error PASSTHROUGH macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef SetPort
	#error SetPort macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef LoadString
	#error LoadString macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef OFFSET
	#error OFFSET macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif
#ifdef ERROR
	#error ERROR macro defined, you have to clean-up after a Windows include by adding #include "undef_win_macros.h" between the Windows and MAXON API includes.
#endif

