#ifndef OBJECT_H__
#define OBJECT_H__

#include "maxon/objectbase.h" // objectbase.h is needed to avoid a dependency on datatype.h (which is included by registry.h) from its entities
#include "maxon/registry.h"

#endif // OBJECT_H__
